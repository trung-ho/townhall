.container.login
  %h3.text-center Make your vote COUNT and UNLOCK cool features !

  - if flash[:alert]
    .text-center.error.color-no.text-italic.large-text= flash[:alert]

%h2 Sign up
= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
  = f.hidden_field :role, value: @role
  = devise_error_messages!
  
  .row.form-group
    .col-xs-4.large-text
      = f.label :email, "Email:"
    .col-xs-8
      = f.email_field :email, autofocus: true, required: true, class: 'form-control'
  .row.form-group
    .col-xs-4.large-text
      = f.label :password, "Password:"
    .col-xs-8
      = f.password_field :password, autocomplete: "off", required: true, class: 'form-control'
  .row.form-group
    .col-xs-4.large-text
      = f.label :password_confirmation, "Password confirmation:"
    .col-xs-8
      = f.password_field :password, autocomplete: "off", required: true, class: 'form-control'
  .row
    .col-xs-6.text-center
      = link_to "Log in", new_session_path(resource_name), class: 'btn btn-yes large-text'
    .col-xs-6.text-center
      = f.submit "Register", class: 'btn btn-info large-text'


/   .field
/     = f.label :email
/     %br/
/     = f.email_field :email, autofocus: true
/   .field
/     = f.label :password
/     - if @validatable
/       %em
/         (#{@minimum_password_length} characters minimum)
/     %br/
/     = f.password_field :password, autocomplete: "off"
/   .field
/     = f.label :password_confirmation
/     %br/
/     = f.password_field :password_confirmation, autocomplete: "off"
/   .actions
/     = f.submit "Sign up"
/ = render "users/shared/links"
