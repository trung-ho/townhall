%h3.login_header.text-center Make your vote COUNT and UNLOCK cool features !
.container.login
  - if flash[:alert]
    .text-center.error.color-no.text-italic.large-text= flash[:alert]

  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = f.hidden_field :role, value: @role
    = devise_error_messages!
    
    .row.form-group
      .col-xs-4.large-text
        = f.label :email, "Email:"
      .col-xs-8
        = f.email_field :email, autofocus: true, required: true, class: 'form-control'
    .row.form-group
      .col-xs-4.large-text
        = f.label :password, "Password:"
      .col-xs-8
        = f.password_field :password, autocomplete: "off", required: true, class: 'form-control'
    / .row.form-group
    /   .col-xs-4.large-text
    /     = f.label :password_confirmation, "Password confirmation:"
    /   .col-xs-8
    /     = f.password_field :password, autocomplete: "off", required: true, class: 'form-control'
    .row.form-group
      .col-xs-8.col-xs-offset-2.text-center
        %p 
          By registering you are accepting the townhall 
          = link_to 'Terms of Use', root_path
        = f.submit "Register", class: 'btn btn-yes large-text'
        %label.reason.checkbox.info
          = f.check_box :remember_me, checked: true
          %span.check-sign
          %span.color-black.small-text.reason-content Remember me
  .col-xs-12.text-center
    %p.decorated
      %span or
    %p.no_account Already have an account?
    = link_to "Sign in", new_session_path(resource_name), class: 'large-text'