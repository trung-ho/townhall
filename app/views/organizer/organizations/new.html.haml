#admin_registrations
  .col-sm-6
    = image_tag 'townhall-logo.png'
    #steps
      %h2 Step 2 of 3
      .progress_bar
        .step.done
        .step.doing
        .step
      
    %p Create your unique address for your townhall.
    %p 
      Choose something that best represents your organization.
      %br
      %small (It's difficult to change this later, so take your time to create what you want.)
    
    #sign_in_form
      = semantic_form_for [:organizer, @organization] do |f|
        .row
          .col-sm-8.no_left_grid
            = f.text_field :slug, class: 'form-control'
            = f.semantic_errors :slug
          .col-sm-4
            %label.our_site .townhallapp.io
        .row
          .col-sm-12
            %small.red Your custom url can only contain letters, numbers and dashes (and must start with a letter).
        = f.submit 'Next', class: 'btn btn-yes'
  .col-sm-6.hidden-xs
    .signup_image= image_tag 'admin/featured-image.png'

