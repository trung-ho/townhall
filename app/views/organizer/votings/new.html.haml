%h1 Create a new Vote
= simple_form_for [:organizer, @voting], :html => { :class => 'form-horizontal' } do |f|
  = render partial: 'organizer/questions/common_fields', locals: {f: f}  
  = f.button :submit
